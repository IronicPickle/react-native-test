import { ViewStyle } from "react-native";

export const misc = {
  screen: {
    flexGrow: 1,
    paddingVertical: 48,
    paddingHorizontal: 32,
    backgroundColor: "#fff",
    alignItems: "center",
  } as ViewStyle,
};

export default { misc };
